import unittest
from server.voip_server import ClientManager

class TestChannels(unittest.TestCase):
    
    def setUp(self):
        pass
        # TODO create client manager here
        #self.clients = ClientManager()

    def test_adding(self):
        pass
        #signal_processingself.channels.add_user_to_channel(1, "127.0.0.1", 50010)
        #self.channels.add_user_to_channel(2, "127.0.0.1", 50011)
        #self.channels.add_user_to_channel(4, "127.0.0.1", 50012)
        #time.sleep(3)
        #print(self.channels.get_list_users_on_chanel(1))
        #self.channels.del_user_from_channel(2, "127.0.0.1", 50011)
        #print(self.channels.get_list_users_on_chanel(1))
        #time.sleep(3)
        #self.channels.add_user_to_channel(2, "127.0.0.1", 50011)
